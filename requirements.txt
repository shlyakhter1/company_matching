# core (required) — fuzzy + tfidf + benchmark + visuals all run offline on these
rapidfuzz>=3.0
scikit-learn>=1.3
matplotlib>=3.7
plotly>=5.18
pandas>=2.0
numpy>=1.24

# 2D map reducer (optional; falls back to t-SNE/PCA from scikit-learn if absent)
umap-learn>=0.5

# local embedding matchers (optional; downloads models from HuggingFace on first use)
sentence-transformers>=3.0
torch>=2.0

# API embedding matchers (optional; also need OPENAI_API_KEY / COHERE_API_KEY)
openai>=1.30
cohere>=5.5
